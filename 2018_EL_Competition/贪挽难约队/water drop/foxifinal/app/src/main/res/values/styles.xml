<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowTranslucentStatus">true</item>

        <!-- enable window content transitions -->
        <item name="android:windowContentTransitions">true</item>

        <!--&lt;!&ndash; specify enter and exit transitions &ndash;&gt;-->
        <!--<delayitem name="android:windowEnterTransition">@transition/explode</delayitem>-->
        <!--<delayitem name="android:windowExitTransition">@transition/explode</delayitem>-->

        <!-- specify shared element transitions -->
        <item name="android:windowSharedElementEnterTransition">
            @transition/change_image_transform</item>
        <item name="android:windowSharedElementExitTransition">
            @transition/change_image_transform</item>
    </style>

    <style name="AppTheme.Splash" parent="AppTheme">
        <item name="android:windowBackground">@mipmap/theme</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="UserTheme" parent="android:Theme.Material.Light">
        <item name="android:windowActionBar">true</item>
        <item name="android:windowFullscreen">false</item>
        <!--<delayitem name="android:actionBarStyle">@style/my_actionbar_style</delayitem>-->
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorPrimary">@color/colorPrimary</item>
    </style>

    <style name="my_actionbar_style" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/colorPrimary</item>
    </style>

</resources>
