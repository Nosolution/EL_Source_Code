<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a10572.activitytest">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity
            android:name=".SecondActivity"
            android:label="专注于自身">
            <intent-filter>
                <action android:name="com.example.a10572.activitytest.ACTION_START" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="com.example.a10572.activittytest.MY_CATEGORY" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GameActivity"
            android:label="舒尔特方格" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/Settings"></activity>
        <activity
            android:name=".FirstActivity"
            android:label="安静的胖虎">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>